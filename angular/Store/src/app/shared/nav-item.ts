
export interface NavItem {
    // navigation link
    href: string;
    // navigation label
    label: string;
    // status
    active: boolean;

    // default constructor

    // constructor(href: string, label: string, active: boolean) {
    //     this.href = href;
    //     this.label = label;
    //     this.active = active;
    // }
}