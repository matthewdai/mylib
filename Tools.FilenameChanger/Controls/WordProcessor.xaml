<UserControl x:Class="Tools.FilenameChanger.Controls.WordProcessor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tools.FilenameChanger.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        
        
        <TextBox x:Name="textBox" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />


        <StackPanel Grid.Row="1" HorizontalAlignment="Right">
            <Button Margin="5" Click="Button_Click">Process</Button>
            <Button Margin="5" Click="ProcessFile_Click">Process File</Button>
        </StackPanel>

    </Grid>
</UserControl>
